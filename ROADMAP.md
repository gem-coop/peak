# We plan to

- release a github action to easily publish namespaced gems from CI
- add paid plans with additional features
- add preview indexes to every namespace, for publishing preview gems
- release a github action to easily publish preview gems for every PR, or every main branch commit
- add cooldown indexes to every namespace
- add private indexes to every namespace, for publishing private gems
- add private cooldown indexes, with adjustable controls for length and security exceptions
- add a meta-index, listing every namespace that contains gems
- add a precompiled gem index, for super-fast installs
