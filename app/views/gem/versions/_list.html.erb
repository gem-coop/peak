<ul class="list-none" style="color: var(--secondary);">
  <% versions.each do |version| %>
    <li>
      <%= version.ref %>
      <%= tag.span version.published_at.strftime("%b %d %Y"), style: "color: var(--primary);" %>
      <%= version_author(version) %>
    </li>
  <% end %>
</ul>
