<%= tag.h1 page.title = "New Push Key" %>

<p>We'll send you a push key to the given email address. It expires in 24 hours and replaces an existing active key, if any.</p>

<p>You can use it to push gems like this:</p>

<pre>
  <code><%= Peak::Command.gem_push "push_key", namespace_url("@namespace") -%></code>
  <code><%= Peak::Command.rake_release "push_key", namespace_url("@namespace") -%></code>
</pre>

<hr>

<%= form_with url: user_push_keys_path do |form| %>
  <%= form.label :email_address %>
  <%= form.email_field :email_address, placeholder: "me@example.com" %>

  <%= form.button "Create push key" %>
<% end %>

<header style="max-width: 80%; margin: 0 auto;">
  <b>NOTE!</b> We have an issue with pushing precompiled gems right now, we'll have it fixed up soon.
</header>
