#!/bin/bash

set -euo pipefail

if [[ $(hostname -s) == "MSE-6" ]]; then
    export JOB_CONCURRENCY=32
    export SOLID_QUEUE_SKIP_RECURRING=true
fi

set -vx
bin/rails r 'CooldownVersion.import'
bin/jobs
