#!/usr/bin/env sh

# shorthand bin script to query our server, like this:
# bin/q versions
# bin/q @gemcoop/versions
# bin/q info/oaken
# bin/q @gemcoop/info/oaken --head

# forwards rest arguments onto curl, so stuff like `bin/q @gemcoop/info/oaken --head` works.
curl http://peak.test/$1 ${@: 2}
