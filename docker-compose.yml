services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: peak
      POSTGRES_HOST_AUTH_METHOD: trust
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U peak"]
      interval: 1s
      timeout: 1s
      retries: 15

volumes:
  postgres_data:
